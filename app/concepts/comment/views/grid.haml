.comments
  = concept('comment/cell', collection: comments, last: comments.last)

  - if !comments.total_pages.zero? && !comments.last_page?
    #next
      = link_to 'More', |
        next_comments_thing_path(id: model, page: comments.next_page), |
        remote: true
